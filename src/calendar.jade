extends _base

block head
  title Calendar | Wissahickon FBLA
  link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/fullcalendar/2.4.0/fullcalendar.min.css")
  
block content
  .row
    .col-xs-12.col-sm-10.col-sm-offset-1
      h1.text-uppercase Calendar
      br
      #calendar

block scripts
  script(src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.10.6/moment.min.js")
  script(src="https://cdnjs.cloudflare.com/ajax/libs/fullcalendar/2.4.0/fullcalendar.min.js")
  script(src="https://cdnjs.cloudflare.com/ajax/libs/fullcalendar/2.4.0/gcal.js")
  script.
    $(document).ready(function() {
      $('#calendar').fullCalendar({
        googleCalendarApiKey: 'AIzaSyCSCSDvcrqWcUnbxPiMoeUnFO06ByOTgog',
        events: {
          googleCalendarId: 'fbla.wissahickon@gmail.com'
        },
        eventClick: function(event) {
          if (event.url) {
            window.open(event.url);
            return false;
          }
        }
      });
    });
